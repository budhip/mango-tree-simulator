# mango-tree-simulator
### Using react.js, firebase, and background job

## Build Setup

``` bash
# install dependencies
npm install

# running client-side
npm start

# running server-side
node mango.js
```
